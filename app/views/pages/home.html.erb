<% if user_signed_in? %>
  <% #Show them something %>
<% else %>
  <div class="hero-unit">
    <h1>Welcome to The Brahminator</h1>
    <p> The only tool you need to live your life as a (better) Brahmin</p>
    <p> <%= link_to "Sign up now", new_user_registration_path, class:"btn btn-primary btn-large" %> </p>
  </div>
<% end %>
